#include<stdio.h>
int main(){
  printf("size of flaot=%ld",sizeof(float));
  printf("size of longint=%ld",sizeof(long int));
  printf("size if shortint=%ld",sizeof(short int));
  printf("size of double=%ld",sizeof(double));
  printf("size if char=%ld",sizeof(char));
  return 0;
}
